# Design-system components

A component is ready when:

1. Tokens wired
2. States documented
3. A11y behavior documented (APG link if composite)
4. Code + Penpot (or explicit ‘code-only’) stay aligned
5. Usage do/don’t in docs
