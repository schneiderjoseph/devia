# Tablet

- Hybrid: touch-first density with optional keyboard
- Avoid hover-only actions (RWD-003, INT-001)
- Tables: prefer card/list alternatives or horizontal scroll with sticky headers + accessible alternative
