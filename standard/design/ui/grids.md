# Grids & breakpoints

Define breakpoints as tokens. Align columns to a consistent grid.
Prefer CSS Grid/Flex as in implementation — Penpot auto-layouts should map cleanly to flex/grid mental models.
See [`responsive/`](../responsive/).
