# UI principles

- Hierarchy: brand/product identity → primary action → supporting content
- Spacing and type scales come from **tokens**, not ad-hoc pixels
- One composition per viewport job (avoid dashboard clutter on simple tasks)
- Prefer full-bleed structure only when the product surface calls for it; otherwise respect content width
- Motion communicates state changes; it is not decoration by default
