# AI instructions

1. Load `AGENTS.md` + relevant `rules/**`
2. Prefer citing **rule IDs**
3. Never invent parallel tokens
4. Return the compliance format in `compliance-format.md`
5. List **Not verified** explicitly
