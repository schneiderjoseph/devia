# 13 — Recipes

> How to do the routine things **in this repository**, in the order that actually works. A recipe
> exists so nobody has to rediscover step five.

## Add an endpoint

```text
1. TODO(devia)
2. ...
n. Update 02_SURFACES.md in the same change
```

## Add a table or a migration

```text
1. TODO(devia)
n. Update 03_DATA_MODEL.md in the same change
```

## Add a screen

```text
1. TODO(devia)
n. Update 02_SURFACES.md and 07_DESIGN.md in the same change
```

## Run the checks

```text
TODO(devia): the real commands — install, dev, test, lint, typecheck, migrate
```

Every recipe ends with the memory update it implies. That is what makes `MEM-009` mechanical
rather than virtuous.
